desc: 基本的なインクルード
runners:
  httpbin: http://localhost:8080

steps:
  - desc: 共通処理をインクルード
    include:
      path: ./common/auth.yml
    
  - desc: インクルード後のテスト
    httpbin:
      /get:
        get:
          query:
            included: "true"
    test: current.res.status == 200