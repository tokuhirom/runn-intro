desc: SSH経由でのリモート操作
runners:
  server: ssh://user@example.com:22
steps:
  # ホスト名を表示
  show_hostname:
    ssh:
      server:///
        command: hostname
    test: current.exit_code == 0
    dump: current.stdout